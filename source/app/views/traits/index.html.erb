<h1> Hi Everyone! </h1>

<% @traits.each do |trait| %>
  <%= form_for(trait) do |f| %>
    <%= f.label trait.name %>
    <input type="range" id="">
    <%# fields_for range_field, trait.current_user_rating, max: 100, min: 0 %>
    <%#= f.check_box %>
    <%= f.submit %>
  <% end %>
<% end %>


